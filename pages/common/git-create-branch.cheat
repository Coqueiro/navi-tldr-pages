; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/git-create-branch.md

% git-create-branch, common

# Create a local branch
git create-branch <branch_name>

# Create a branch locally and on origin
git create-branch <__r___remote_> <branch_name>

# Create a branch locally and on upstream (through forks)
git create-branch <__r___remote_> upstream <branch_name>

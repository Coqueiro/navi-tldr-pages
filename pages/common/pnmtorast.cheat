; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/pnmtorast.md

% pnmtorast, common

# Convert a PNM image to a RAST image
pnmtorast <path_to_input_pnm> > <path_to_output_rast>

# Force either `RT_STANDARD` or `RT_BYTE_ENCODED` form for the output
pnmtorast -<standard_rle> <path_to_input_pnm> > <path_to_output_rast>

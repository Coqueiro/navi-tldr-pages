; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/jj-config-path.md

% jj-config-path, common

# Print the path to the user-level config file
jj config <_p_path_> --user

# Print the path to the repo-level config file
jj config <_p_path_> --repo

# Print the path to the workspace-level config file
jj config <_p_path_> --workspace

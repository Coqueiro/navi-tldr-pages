; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/sc_warts2pcap.md

% sc_warts2pcap, common

# Convert the data from several `warts` files into one PCAP file
sc_warts2pcap -o <path_to_output_pcap> {{path/to/file1.warts path/to/file2.warts ...}}

# Convert the data from a `warts` file into a PCAP file and sort the packets by timestamp
sc_warts2pcap -s -o <path_to_output_pcap> <path_to_file_warts>

; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/dolt-sql.md

% dolt-sql, common

# Run a single query
dolt sql <__q___query_> "{{INSERT INTO t values (1, 3);}}"

# List all saved queries
dolt sql <__l___list_saved_>

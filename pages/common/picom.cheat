; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/picom.md

% picom, common

# Enable `picom` during a session
picom &

# Start `picom` as a background process
picom <__b___daemon_>

# Use a custom configuration file
picom --config <path_to_config_file>

; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/kubectl-exec.md

% kubectl-exec, common

# Open Bash in a pod, using the first container by default
kubectl exec <pod_name> {{[-it|--stdin --tty]}} -- bash

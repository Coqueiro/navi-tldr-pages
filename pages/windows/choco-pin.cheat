; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/windows/choco-pin.md

% choco-pin, windows

# Display a list of pinned packages and their versions
choco pin list

# Pin a package at its current version
choco pin add <__n___name_> <package>

# Pin a package at a specific version
choco pin add <__n___name_> <package> --version <version>

# Remove a pin for a specific package
choco pin remove <__n___name_> <package>

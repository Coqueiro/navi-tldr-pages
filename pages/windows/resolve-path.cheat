; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/windows/resolve-path.md

% resolve-path, windows

# Resolve the home folder path
Resolve-Path <_>

# Resolve a UNC path
Resolve-Path -Path "\<hostname>\{{path	oile}}"

# Get relative paths
Resolve-Path -Path {{path	oile_or_directory}} -Relative

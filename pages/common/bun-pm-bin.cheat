; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/bun-pm-bin.md

% bun-pm-bin, common

# Get the path to the local project's `bin` directory
bun pm bin

# Get the path to the global `bin` directory
bun pm bin <__g___global_>

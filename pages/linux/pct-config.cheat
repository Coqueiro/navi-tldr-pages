; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/pct-config.md

% pct-config, linux

# Display the saved configuration
pct <_conf_config_> <100>

# Display the running configuration without pending changes
pct <_conf_config_> <100> --current

# Display configuration of a specific snapshot
pct <_conf_config_> <100> --snapshot <snapshot_name>

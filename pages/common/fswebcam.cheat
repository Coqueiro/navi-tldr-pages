; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/fswebcam.md

% fswebcam, common

# Take a picture
fswebcam <filename>

# Take a picture with custom resolution
fswebcam <__r___resolution_> <width>x<height> <filename>

# Take a picture from selected device(Default is `/dev/video0`)
fswebcam <__d___device_> <device> <filename>

# Take a picture with timestamp(timestamp string is formatted by strftime)
fswebcam --timestamp <timestamp> <filename>

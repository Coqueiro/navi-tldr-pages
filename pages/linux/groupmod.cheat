; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/linux/groupmod.md

% groupmod, linux

# Change the group name
sudo groupmod <__n___new_name_> <new_group> <group_name>

# Change the group ID
sudo groupmod <__g___gid_> <new_id> <group_name>

; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/vagrant-reload.md

% vagrant-reload, common

# Reload the currently running machine
vagrant reload

# Target a machine by name or ID
vagrant reload <name_id>

# Force the provisioners to run
vagrant reload --provision

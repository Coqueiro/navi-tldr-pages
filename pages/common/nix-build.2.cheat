; This has been extracted from
; https://github.com/tldr-pages/tldr/blob/master/pages/common/nix-build.2.md

% nix-build.2, common

# Build a Nix expression
nix-build '<nixpkgs>' <__A___attr_> <firefox>

# Build a sandboxed Nix expression (on non-NixOS)
nix-build '<nixpkgs>' <__A___attr_> <firefox> --option sandbox true
